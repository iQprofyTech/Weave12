FROM node:20-alpine
RUN apk add --no-cache curl
WORKDIR /app
COPY package*.json ./
RUN npm install --ignore-scripts
COPY . .
RUN npm install --ignore-scripts
WORKDIR /app/apps/api
CMD ["npm","run","start"]
